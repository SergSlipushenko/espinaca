return {
    {every = 1, job = 'dumb', wifi=false, mqtt=false, sntp = false},
   -- {every = 5, job = 'time_sync', wifi=true, mqtt=false, sntp = true},
   -- {every = 2, job = 'send_data', wifi=true}
}
